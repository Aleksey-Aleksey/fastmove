{
  "name": "fastmove",
  "source": "src/index.html",
  "homepage": "https://aleksey-aleksey.github.io/fastmove",
  "scripts": {
    "start": "parcel",
    "build": "parcel build src/index.html --dist-dir dist --public-url ./",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "parcel": "^2.15.4"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.14.3",
    "gh-pages": "^6.3.0",
    "svgo": "^3.3.2"
  }
}
