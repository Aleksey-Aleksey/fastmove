{
  "name": "fastmove",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "npx parcel build src/index.html --dist-dir dist --public-url ./"
  },
  "dependencies": {},
  "devDependencies": {
    "@parcel/transformer-sass": "^2.14.3",
    "parcel": "^2.9.3"
  },
  "browserslist": [
    "last 2 versions",
    "not dead"
  ]
}
